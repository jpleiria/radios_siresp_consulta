# Consulta Rádios SIRESP — CBSLeiria

Página de consulta que lê `dados-siresp.json` diretamente da branch `main` através do GitHub Raw.

Para atualizar os dados, substitua apenas `dados-siresp.json`. O código de acesso é uma barreira visual; o repositório e o JSON permanecem públicos.
